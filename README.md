# platform-ci

Reusable workflows

1. Create the 3 reusable workflow files

2. Commit to main

3. Create tag v1:

### Tags:

- v1 ←─ Points to commit SHA
- v1.1

```bash
git tag -a v1 -m "Initial version"
git push origin v1
```
